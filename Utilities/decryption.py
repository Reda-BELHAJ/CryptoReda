LIMITS_Upp = (65, 90)
LIMITS_Dwn = (97, 122)

def hasNumbers(inputStr):
    return any(char.isdigit() for char in inputStr)

def decrypt(input: str, key: str):
    if hasNumbers(key):
        pass
    else:
        pass


input = "tedasTscab"
key = "1dad54"
key2 = "dsa"
